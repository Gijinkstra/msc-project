# Reduced Order Modelling of a Cello Body Transfer Function

This repository houses the code utilised to decompose a cello body transfer function via the Eigensystem Realisation Algorithm (ERA).

The ERA uses a Hankel matrix construction from a single impulse response measurement and constructs a minimal realisation of the state space triple **A**, **B** and **C**.